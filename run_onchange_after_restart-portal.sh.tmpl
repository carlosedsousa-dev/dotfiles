#!/bin/sh
# checksum: {{ sha256sum (include "dot_config/xdg-desktop-portal/niri-portals.conf") }}
# Reinicia o portal de desktop quando o config do niri muda,
# para o seletor de arquivo (FileChooser) apontar pro backend GTK.
systemctl --user try-restart xdg-desktop-portal.service || true
